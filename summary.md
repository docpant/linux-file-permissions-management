# Summary

In this project, Linux commands were used to review and modify file and directory permissions according to organizational security requirements. File permissions were analyzed using `ls -la`, and unauthorized access permissions were removed using `chmod`.

The project also included securing hidden files and restricting directory access to authorized users only. These tasks demonstrate practical Linux access control management and support the principle of least privilege to improve system security.
